/*
 ..identifiers
 ..Name convention And Rules => Rules: can use letters, digits, $
  and _, cannot start with a number, case-sensitive,
   and cannot use reserved words
 ..Reserved Words =>Reserved Words are keywords that
  have a special meaning in JavaScript.
You cannot use them as variable names,
function names, or identifiers.

*/
var user = "elqemary";
console.log(user);